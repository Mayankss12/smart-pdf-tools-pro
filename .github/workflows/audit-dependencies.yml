name: Harden dependencies

on:
  push:
    branches:
      - codex/dependency-security-hardening

permissions:
  contents: write

jobs:
  harden:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/dependency-security-hardening
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Upgrade framework and remove unused vulnerable dependency
        shell: bash
        run: |
          npm uninstall fabric
          npm install next@16.3.1 react@latest react-dom@latest --save-exact
          npm install --save-dev --save-exact eslint@9.39.5 eslint-config-next@16.3.1 postcss@8.5.26 @types/react@latest @types/react-dom@latest
          npm pkg set scripts.lint="eslint ."
          npm audit fix --package-lock-only
          npm ci
      - name: Security gate
        shell: bash
        run: |
          npm audit --json > audit.json || true
          node - <<'NODE'
          const fs = require('fs');
          const audit = JSON.parse(fs.readFileSync('audit.json', 'utf8'));
          const counts = audit.metadata?.vulnerabilities ?? {};
          console.log(JSON.stringify({ vulnerabilities: counts, details: audit.vulnerabilities }, null, 2));
          if ((counts.critical ?? 0) > 0 || (counts.high ?? 0) > 0) process.exit(1);
          NODE
      - name: Lint
        run: npm run lint
      - name: TypeScript
        run: npx tsc --noEmit
      - name: Smoke tests
        run: npm run test:smoke
      - name: Production build
        run: npm run build
      - name: Commit validated dependency update
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json package-lock.json
          git diff --cached --quiet && exit 0
          git commit -m "Patch vulnerable runtime dependencies"
          git push origin HEAD:codex/dependency-security-hardening
